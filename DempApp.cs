





/*using DemoApp;
using static program;
class program
{
    public static void Main(string[] args)
    {
        List<Employee> employees = DataManger.GetData();

        //particular data fetch in collection 

        employees.Where(e => e.Age > 0).ToList();






        //collection order /short 

       //employees= employees.OrderBy(e => e.FirstName).ToList();

       //IEnumerable<String>Names = Employee.Select(e => e.FirstName,e=>e.LastName);
        //foreach(String Name in Names) { Console.WriteLine(Name); }  

        //employees.ForEach(employee => Console.WriteLine("ID: {0}, FirstName: {1}, LastName:{2},Age:{3},Department:{4},Salary:{5}"
           //  , employee.ID, employee.FirstName, employee.LastName, employee.Age, employee.Department, employee.Salary));
        

        /* foreach (Employee employee in employees)
         {
             Console.WriteLine("ID: {0}, FirstName: {1}, LastName:{2},Age:{3},Department:{4},Salary:{5}"
              , employee.ID, employee.FirstName, employee.LastName, employee.Age, employee.Department, employee.Salary);
         }
     }
    }
}*/

using System.Collections;
using System. Collections .Generic;

